server:
  port: "8080"
  mode: "release"
  trusted_proxies: "" # 逗号分隔或 CIDR；留空表示不信任代理头

database:
  type: "sqlite" # sqlite, mysql, postgres
  filename: "database/perfect_pic.db" # for sqlite; independent from --config-dir
  host: "127.0.0.1" # for mysql/postgres
  port: "3306"
  user: "root"
  password: "password"
  name: "perfect_pic"
  ssl: false # enable TLS/SSL for mysql/postgres

jwt:
  secret: "perfect_pic_secret"
  expiration_hours: 24

upload:
  path: "uploads/imgs"
  url_prefix: "/imgs/"
  avatar_path: "uploads/avatars"
  avatar_url_prefix: "/avatars/"

smtp:
  host: "smtp.example.com"
  port: 587
  username: "examle@example.com"
  password: "your_smtp_password"
  from: "examle@example.com"
  ssl: false

redis:
  enabled: false
  addr: "127.0.0.1:6379"
  password: ""
  db: 0
  prefix: "perfect_pic"
